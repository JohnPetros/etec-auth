const PREFIX = '@etec-auth'
const USER_KEY = `${PREFIX}:user`
const USER_EMAIL_KEY = `${PREFIX}:user_email`
const TOKEN_KEY = `${PREFIX}:token`
const REFRESH_TOKEN_KEY = `${PREFIX}:refresh_token`

export { USER_KEY, USER_EMAIL_KEY, TOKEN_KEY, REFRESH_TOKEN_KEY }
